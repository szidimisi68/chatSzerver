<Window x:Class="chat.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:chat"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="50px"></RowDefinition>
        </Grid.RowDefinitions>

        <ListBox x:Name="lbChat"></ListBox>
        <ListBox x:Name="lbUsers" Grid.Column="1" Grid.RowSpan="2"></ListBox>
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <TextBox x:Name="tbxMessage" Width="430px" Height="30" HorizontalAlignment="Left" FontSize="14" Margin="10"></TextBox>
            <Button x:Name="btnSend" Height="30" Margin="10" Padding="10 0" Click="btnSend_Click">Küldés</Button>
        </StackPanel>
        <Button x:Name="btnRefresh" Grid.Row="1" Grid.Column="1" Margin="10" Click="btnRefresh_Click">Frissít</Button>
    </Grid>
</Window>